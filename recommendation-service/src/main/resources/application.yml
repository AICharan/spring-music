server:
  port: 8082

spring:
  application:
    name: recommendation-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.cloudfoundry.samples.recommendations: INFO
